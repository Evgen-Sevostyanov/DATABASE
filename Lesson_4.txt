sqlite> UPDATE streams SET SET started_at = SUBSTR(started_at, 7, 4) || '-' || SUBSTR(started_at, 4, 2) || '-' || SUBSTR(started_at, 1, 2);

sqlite> SELECT * FROM streams ORDER BY started_at DESC LIMIT 1;

sqlite> SELECT DISTINCT(substr(started_at, 1, 4)) FROM streams;

sqlite> SELECT id, MAX(id) AS 'total_teachers' FROM teachers ORDER BY id DESC;

sqlite> SELECT * FROM streams ORDER BY started_at DESC LIMIT 2;

sqlite> SELECT AVG(perfomance) FROM grades WHERE teachers_id = 1;














































































































































































































































